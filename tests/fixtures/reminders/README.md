These fixtures are synthetic Reminders CloudKit payloads.

They are manually minimized from local CloudKit capture shapes and intentionally
avoid raw HAR data, cookies, account identifiers, signed URLs, reminder content,
and binary media. IDs, CRDT blobs, timestamps, change tags, and URLs are fake
and stable for tests.
